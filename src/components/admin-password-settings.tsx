"use client";
import {FormEvent,useState} from "react";
import {LockKeyhole} from "lucide-react";
export default function AdminPasswordSettings(){const[currentPassword,setCurrent]=useState(""),[newPassword,setNew]=useState(""),[confirm,setConfirm]=useState(""),[error,setError]=useState(""),[saving,setSaving]=useState(false);async function submit(e:FormEvent){e.preventDefault();setError("");if(newPassword!==confirm)return setError("The new passwords do not match.");setSaving(true);const r=await fetch("/api/banking/profile/password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({currentPassword,newPassword})}),d=await r.json();setSaving(false);if(!r.ok)return setError(d.error);window.location.assign("/admin-login")}return <section><div className="flex items-center gap-3"><span className="grid size-11 place-items-center rounded-xl bg-bank-50 text-bank-700"><LockKeyhole size={20}/></span><div><h2 className="text-2xl font-bold">Change password</h2><p className="text-xs text-neutral-500">You will be signed out after changing the administrator password.</p></div></div><form onSubmit={submit} className="card mt-5 grid gap-5 rounded-3xl p-6 sm:grid-cols-2"><label className="sm:col-span-2"><span className="label">Current password</span><input required type="password" autoComplete="current-password" className="field" value={currentPassword} onChange={e=>setCurrent(e.target.value)}/></label><label><span className="label">New password</span><input required minLength={10} type="password" autoComplete="new-password" className="field" value={newPassword} onChange={e=>setNew(e.target.value)}/></label><label><span className="label">Confirm new password</span><input required minLength={10} type="password" autoComplete="new-password" className="field" value={confirm} onChange={e=>setConfirm(e.target.value)}/></label><p className="text-xs text-neutral-500 sm:col-span-2">Use at least 10 characters with uppercase, lowercase, and a number.</p>{error&&<p className="text-sm text-red-700 sm:col-span-2">{error}</p>}<button disabled={saving} className="btn sm:col-span-2"><LockKeyhole size={16}/>{saving?"Updating...":"Update password"}</button></form></section>}

// Hostinger source snapshot sync.
